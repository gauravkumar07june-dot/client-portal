// Single source of truth for cross-site branding/contact values. Swap the
// values below per client deployment — nothing else needs to change.
const siteConfig = {
  companyName: 'Meridian Construction Group',
  // Full international format, digits only, no "+", no spaces/dashes
  // (e.g. "919876543210" for a +91 India number). Placeholder for now.
  whatsappNumber: '10000000000',
  contactEmail: 'projects@meridianconstruction.example',
}

export default siteConfig
